@keyframes slideInUpConst {
  from {
    transform: translate3d(0, 100vh, 0);
    visibility: visible;
  }

  to {
    transform: translate3d(0, 0, 0);
  }
}

.slideInUpConst {
  animation-name: slideInUpConst;
}
